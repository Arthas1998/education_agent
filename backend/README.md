# Backend

Run the Flask development server:

```powershell
python manage.py run --debug
```

Pre-convert all PDFs to images:

```powershell
python manage.py preproc
```

Requirements are listed in `backend/requirements.txt`.

